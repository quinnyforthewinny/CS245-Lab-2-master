#ifndef DECRYPT_H
#define DECRYPT_H

#include "stdafx.h"
#include <string>

using namespace std;

// Function declaration:z
// decrypt(string &cipher_text, unsigned key)

string decrypt(const string &, int);

#endif